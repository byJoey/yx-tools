# ==============================================
# CloudflareSpeedTest 环境变量配置文件
#小白建议只修改github_token和github_repo，有能力的测速url尽量自建（cloudflareR2+缓存模式很好用）

#自建测速url方法：
#准备一个文件 使用压缩 压缩成7z格式 大小200-300M
#cloudflare R2新建个桶名字随便 一定要设置自定义域名否则限速
#上传7z文件，文件名相当于uuid或者密码 一定不要用200m.7z这类名字 否则被扫了会被刷流量 因为r2不带路径访问域名是无法访问的
#chech rule 设置默认格式缓存规则  边缘ttl缓存记得选择"忽略头使用以下时间" 时间拉长  浏览器缓存选择绕过
#然后把 "https://自定义域名/文件名.7z" 填入下方的url参数里

#大佬可以改py文件里的TOOL_TYPE换原版cfst，自己适配参数
# 说明：
# 1. 取消注释并设置参数后，会覆盖脚本的自定义参数
# 2. 未设置的参数将使用脚本中的 CUSTOM_PARAMS（你的自定义参数）
# 3. 若未设置任何环境变量，将完全使用 CUSTOM_PARAMS 中的配置
# ==============================================

# -------------------- 延迟测速相关参数 --------------------
# 延迟测速线程（默认50，最大1000）因为这个版本在docker自动运行，没那么着急，所以默认把并发数调低避免网络波动和被运营商搞
# n=50

# 单个IP延迟测速次数（默认4次）
# t=4

# 测速端口（默认443）
# tp=443

# HTTPing有效状态码（默认200 301 302，仅限一个）
# httping-code=200

# 匹配指定地区（IATA码，逗号分隔，仅HTTPing模式可用）
# 例：Cloudflare用HKG,LAX；CDN77用US,CN
# cfcolo=HKG,LAX,SIN

# 启用HTTPing模式（默认禁用，使用TCPing）
# 开关参数：设为true/1/yes启用，其他值禁用
# httping=true


# -------------------- 下载测速相关参数 --------------------
# 下载测速数量（默认10个，从最低延迟开始）
# dn=10

# 单个IP下载测速最长时间（秒，默认10）
# dt=10

# 测速地址（默认https://cf.xiu2.xyz/url，建议自建）
#url=https://cf.xiu2.xyz/url


# -------------------- 过滤条件相关参数 --------------------
# 平均延迟上限（ms，默认9999，docker脚本默认1000）
# tl=1000

# 平均延迟下限（ms，默认0）
# tll=0

# 丢包率上限（0.00~1.00，默认1.00，0表示不允许丢包）
# tlr=0.2

# 下载速度下限（MB/s，官方默认0.00，docker脚本默认1）
# sl=1


# -------------------- 输出与文件相关参数 --------------------
# 显示结果数量（默认10，0表示不显示）
# p=10

# IP段数据文件路径（官方默认ip.txt,docker脚本默认Cloudflare.txt）
# f=Cloudflare.txt

# 直接指定IP段（逗号分隔，如1.1.1.1,2.2.2.2/24）
# ip=1.1.1.1,2.2.2.2/24

# 结果输出文件（默认result.csv，空值表示不写入）
# o=result.csv


# -------------------- 其他控制参数 --------------------
# 禁用下载测速（默认禁用，启用后按延迟排序）
# 开关参数：设为true/1/yes启用
# dd=false

# 测速全部IP（默认禁用，仅IPv4，默认每个/24段随机测1个）
# 开关参数：设为true/1/yes启用
# allip=false

# 调试模式（默认禁用，输出更多日志）
# 开关参数：设为true/1/yes启用
# debug=false


# -------------------- 工具额外配置 --------------------
# GitHub加速代理（默认http://gh-proxy.com/）
# GITHUB_PROXY=http://gh-proxy.com/

# GitHub上传配置（可选，用于自动上传结果）
# GITHUB_TOKEN=your_github_token
# GITHUB_REPO=your_username/your_repo


# GitHub个人访问令牌（需要repo权限）
#GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 仓库标识（格式：用户名/仓库名）
#GITHUB_REPO=JacksonJiangxh/Clash-Rule

# GitHub加速域名（可选，默认使用https://gh-proxy.com/）
# 常用备选：https://fastly.jsdelivr.net/、https://testingcf.jsdelivr.net/、https://cdn.jsdelivr.net/
# GITHUB_PROXY=https://gh.api.99988866.xyz/ 